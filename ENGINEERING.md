# 空耳卡拉OK流水线 · 工程手册

把人手写的中文空耳歌词，逐字对齐到日文原曲的演唱时间轴上，压制成带 KTV 逐字变色的成品视频。

首次落地对象：**打上花火**（DAOKO × 米津玄師），B 站 MV `BV1F8411G7TJ`。

本手册记录的是**实际跑通的流程**，不是设计意图。每条结论后面都附了它的证据来源；凡是估算值都明确标注为估算。

---

## 0. 问题定义

输入：
- 一支包含目标歌曲的视频（B 站 MV）
- 一份**人写的中文空耳歌词**，按作者自己的断句分行

输出：
- 一支硬压字幕的视频，空耳歌词以**逐字变色卡拉OK**形式呈现

难点在于中间那一步。强制对齐工具给出的是「**日文音节 → 时间**」，而屏幕上要显示的是中文汉字。两者：

1. **数量不等**：观察到的实例中，一行 7 个汉字对应 10 个日文音拍
2. **断句不等**：作者的分行与原曲/ASR 的分行都不一致
3. **没有现成工具**：这一层映射是本项目唯一需要自研的部分

关键洞察：**空耳本身就是照着"听起来像"写的，所以音素相似度就是最强的对齐信号。** 整套解法建立在这个前提上。

---

## 1. 系统架构

```
B站检索(WBI签名) ──> 候选抽帧实测 ──> 人工选片 ──> 下载 MV
                                                      │
                                                      ▼
                                              抽音轨 (44.1k stereo)
                                                      │
                                    BS-RoFormer 人声分离 (GPU)
                                                      │
                        ┌─────────────────────────────┼──────────────┐
                        ▼                             ▼              │
              Whisper large-v3 ASR              人声能量包络          │
                        │                        (门控用)            │
                        ▼                             │              │
              日文文本 ──cutlet/pykakasi──> 罗马音     │              │
                        │                             │              │
                        ▼                             │              │
        yohane 强制对齐 (MMS-300m ja-Latn 微调)        │              │
                        │                             │              │
                        ▼                             ▼              │
              [日文音节 → 浮点时间] ──── 幽灵音节剔除 ──┘              │
                        │                                            │
                        ▼                                            │
    ┌───────────────────────────────────────┐                        │
    │  跨语言音素特征空间 + 子序列 DTW        │ <── 人写的中文空耳      │
    │  (本项目自研)                          │                        │
    └───────────────────────────────────────┘                        │
                        │                                            │
                        ▼                                            │
              [空耳汉字 → 逐字时间]                                   │
                        │                                            │
              ┌─────────┴─────────┐                                  │
              ▼                   ▼                                  │
      基础 .ass (\kf)      PyonFX 逐字特效 (32 种)                     │
              │                   │                                  │
              └─────────┬─────────┘                                  │
                        ▼                                            ▼
              libass 渲染 + NVENC 压制 <───────────────────────── 原 MV
                        │
                        ▼
              抽帧 QC (必须肉眼看) ──> 成品
```

---

## 2. 环境

### 2.1 算力放在哪

集群 `hgcc` 的 `sinfo` 显示所有节点 `GRES=(null)`，**没有 GPU**。本地 RTX 2070 SUPER (8 GB, sm_75) 是唯一 GPU。因此：

| | 位置 |
|---|---|
| 全部 ML 计算 | 本地 |
| 工程主目录 / 交付 | 服务器 `funny/konger/dashanghuahuo/` |

### 2.2 组件

装在 `C:/Users/59827/karaoke/`：

| 组件 | 版本 | 用途 |
|---|---|---|
| `tools/ffmpeg/` | master (n-125829) | 通用（抽帧、转码） |
| `tools/ffmpeg-shared71/` | **n7.1.5** | ① torchcodec 的动态库 ② **压制**（见 §6.7） |
| `venv/` | Python 3.12.2 | |
| torch / torchaudio / torchvision | **2.13.0+cu126** / 2.11.0+cu126 / 0.28.0+cu126 | |
| torchcodec | 0.15.0+cu126 | yohane 读音频 |
| audio-separator | 0.44.5 | 人声分离 |
| yohane | git `2026.5.1.dev36` | 强制对齐 |
| PyonFX | — | 逐字特效的字体度量 |
| cutlet + unidic-lite / pykakasi | — | 日文→罗马音（双路） |
| pypinyin | 0.55.0 | 汉字→拼音 |

### 2.3 安装顺序是有讲究的

```bash
# ❌ 错误顺序（本项目实际踩过）
pip install torch --index-url .../cu124      # 装了 CUDA 版
pip install "audio-separator[gpu]"           # ← pip 从默认 PyPI 把 torch 覆盖成 CPU 版
# 结果: torch 2.13.0+cpu, torchaudio 还是 +cu124 → ABI 不匹配
#       libtorchaudio.pyd 加载失败, torch.cuda.is_available() == False

# ✅ 正确顺序
pip install "audio-separator[gpu]"
pip install --index-url https://download.pytorch.org/whl/cu126 \
    "torch==2.13.0+cu126" torchaudio torchvision
```

**为什么锁 cu126 而不是更新的 cu130**：`onnxruntime-gpu` 是 CUDA 12 系。torch 用 CUDA 13、onnxruntime 用 CUDA 12，同进程混用会出问题。驱动 591.86 (CUDA 13.1) 向下兼容 12.6，所以 cu126 是安全解。

**FFmpeg 必须装两份**：
- torchcodec 只支持 FFmpeg **4–8**。BtbN 的 `master` 构建是 `avcodec-63`（FFmpeg 9 开发版），**太新，加载失败**。必须另下 `n7.1` 的 shared 构建。
- 压制也得用 7.1，原因见 §6.7。

---

## 3. 逐阶段规范

每一阶段给出：输入 / 输出 / 命令 / **验收标准**。

### 阶段 1 · B 站检索

| | |
|---|---|
| 脚本 | `bili_search.py` |
| 输出 | `00_source/bilibili_candidates.json` |
| 验收 | 返回条数 > 0 且 `bvid` 非空 |

```bash
python bili_search.py "打上花火" 2
```

**不能用 yt-dlp 的 `bilisearch`**：它既不带 `buvid3` cookie 也不做 WBI 签名，直接 `HTTP 412 Precondition Failed`（本项目实测复现）。

自实现的三步：
1. `GET https://www.bilibili.com/` + `GET /x/frontend/finger/spi` 拿 `buvid3`/`buvid4`
2. `GET /x/web-interface/nav` 取 `img_key`/`sub_key`，按 64 位固定置换表算 `mixin_key`
3. `w_rid = md5(sorted_query + mixin_key)`，打 `/x/web-interface/wbi/search/type`

正片提取**不受此限**，`yt-dlp -J <BV url>` 可直接用。

### 阶段 2 · 候选实测

| | |
|---|---|
| 脚本 | `probe_candidates.py` → `frame_grab.py` |
| 输出 | `00_source/candidate_meta.json`, `05_qc/probe/*.jpg` |
| 验收 | **阳性对照必须被检出** |

B 站的 metadata 里**没有任何字段**能说明视频是否内嵌硬字幕。只能抽帧看。

**方法学要求**：候选集里必须放一个标题明写"（中日歌词）"的片子作为**阳性对照**。只有确认检测器能看出它有字幕，其余候选的"干净"判定才可信。

本次实测的收获（都是 metadata 骗不出来的）：
- 一个标题写着歌名的候选，抽出来是**一套电子鼓的实拍**
- 一个标题完全没提字幕的候选，实际有中日双语硬字幕 + 两处水印
- 一个"Hi-Res"候选其实是音乐可视化样式（静态图 + 进度环 + 滚动歌词）

抽帧时机也有讲究：要选在**确定有人声的时段**，不能机械取 25%/50%/75%，否则可能抽到间奏——间奏没字幕，会把有硬字幕的片子误判成干净的。

### 阶段 3 · 下载

```bash
yt-dlp --ffmpeg-location <ffmpeg/bin> \
  -f "bv*[height<=1080][vcodec^=avc1]+ba/b[height<=1080]" \
  --merge-output-format mp4 -o "BV....%(ext)s" <url>
```

验收：`ffprobe` 确认分辨率/帧率/时长/音频码率符合预期。

> BBDown 已于 2026-05-14 归档停维，不要再用。

### 阶段 4 · 人声分离

| | |
|---|---|
| 脚本 | `separate_vocals.py` |
| 模型 | `model_bs_roformer_ep_368_sdr_12.9628.ckpt` |
| 输出 | `01_stems/song_(Vocals)_*.wav`, `..._(Instrumental)_*.wav` |

**模型选择**：包默认是 `ep_317`。但仓库自己的 `audio_separator/models-scores.json` 在同一批 40 首曲子上的实测中位 vocals SDR 是：

| 模型 | 实测中位 SDR |
|---|---|
| `ep_368_sdr_12.9628` | **12.102** |
| `ep_317_sdr_12.9755`（默认） | 11.774 |

文件名里的 `12.9755` 是作者训练时的自报值，不是这个 benchmark 的结果。同架构同大小（639 MB），换 `ep_368` 是白捡的 0.33 dB。

**验收标准 —— 必须验"真的分离了"，不能只看有没有输出文件**：

| 时段 | 人声轨 RMS | 伴奏轨 RMS |
|---|---|---|
| 前奏 2–15s | **0.00000** | 0.031 |
| 主歌 35–50s | 0.061 | 0.080 |
| 副歌 100–115s | 0.081 | 0.069 |
| 尾奏 285–296s | **0.00000** | 0.001 |

纯器乐段人声轨归零、演唱段有能量 —— 这才叫分离生效。

> `voc + ins` 与原混音有约 0.076 的 RMS 残差，是 `--normalization 0.9` 对输出重缩放导致的，不影响用途。

**内存**：分块推理，`chunk = stft_hop(441) × (dim_t 801 − 1) = 352800 samples = 8.000 s`，所以 VRAM **与歌曲长度无关**。

### 阶段 5 · ASR 与罗马音

| | |
|---|---|
| 脚本 | `transcribe_vocals.py` |
| 模型 | `openai/whisper-large-v3`, fp16 |
| 输出 | `02_lyrics/asr_segments.json`, `lyrics_romaji.txt`, `romaji_check.json` |
| 实测峰值显存 | **4.51 GB** |

**为什么用 ASR 而不是抓歌词站**：
1. 强制对齐要的是**实际唱出来的东西**。纸面歌词与演唱常有出入（省略助词、重复段、气声、rap 段断句），用纸面词对齐会在这些地方崩。
2. ASR 的分句天然贴合演唱停顿。
3. 不依赖第三方站点的可用性与反爬。

**必须转罗马音**（见 §6.3）。双路转换交叉验证：`cutlet`（MeCab 形态分析）为主，`pykakasi`（词典法）为辅。

比较时**要忽略空格**——两者分词策略不同。本次结果：
- 按词比较：一致 2/14（误导性指标）
- 忽略空格的字母序列比较：**完全相同 9/14，平均相似度 0.9892**

**Whisper 的分段时间戳不可靠**（观察到一段跨 68 秒，末段结束时间 310s 超过 298.3s 的片长）。但这对本流水线**无害**，因为所有时间轴都由下一阶段重算，Whisper 只提供文本。

### 阶段 6 · 强制对齐

| | |
|---|---|
| 脚本 | `force_align.py` |
| 模型 | `NextFire/mms-300m-ForcedAligner-karaoke-ja-Latn` (~1.18 GiB) |
| 输出 | `02_lyrics/syllables_ja.json` |
| 实测峰值显存 | **7.17 GB / 8.59 GB** |

**走库不走 CLI**：CLI 只会渲染 `.ass`。我们需要浮点数好重新映射到中文字上。`yohane.subtitles.time_lyrics()` 返回：

```python
@dataclass
class TimedSyllable:
    value: str; start_s: float; end_s: float
```

**显存警告**：对齐器**不分块**（`emission, _ = self.model(waveform)` 整首一次前向），VRAM 随时长**线性增长**，且全程 fp32 无 autocast。

> 调研阶段的算术估算是 3–4 GB，**实测 7.17 GB**，翻了一倍多。这就是为什么脚本里要实测并打印峰值，而不是照抄估算值。约 5 分钟的曲子已接近 8 GB 卡的上限。

### 阶段 7 · 空耳配对（自研核心）

见 §4。

### 阶段 8 · 字幕生成

**默认走滚动扫光**（`make_ass.py`）。逐字动画引擎（`effects.py` / `render_effects.py` / `make_mixed.py`）是**可选项**，需要时才用，见 §5。

| 方式 | 一行的 Dialogue 数 | 字会不会离开原位 | 适用 |
|---|---|---|---|
| **滚动扫光 `\kf`（默认）** | 1 | 否 | 长时间观看、真正跟唱 |
| 逐字动画（`render_effects.py`） | 3 × 字数 | 视特效而定 | 短视频、需要强视觉冲击 |

**配色方案如何与滚动扫光共存**：样式里把 `SecondaryColour` 设成白色（未唱），然后在每个字前面内联一个 `\1c` 覆盖 `PrimaryColour`：

```
{\1c&H00FFC8B4&\kf34}好{\1c&H00FFC0A0&\kf24}难...
```

于是"彩虹 / 寒冰 / 火焰"这些**逐字渐变配色**在滚动扫光下照样成立 —— 不需要动用逐字动画引擎。这是一个重要区分：**颜色类效果不需要逐字 Dialogue，动画类才需要。**

ASS 卡拉OK的颜色语义**容易搞反**：

| 字段 | 含义 |
|---|---|
| `SecondaryColour` | **还没唱到**时的颜色 |
| `PrimaryColour` | **已经唱过**之后的颜色 |

所以"白字唱到变色"要写成 `Secondary=白, Primary=目标色`。

`\kf` 是平滑扫过，`\k` 是瞬间跳色，单位都是**厘秒**。

**`\kf` 时长必须 gapless**：每个字的值 = **下一个字的起点** − 本字起点，而不是本字自己的 `end`。否则字与字之间的空隙会让扫光停顿。最后一个字才用自己的 `end`。

### 阶段 9 · 压制与 QC

```bash
python burn.py ice_rainbow --trim=2.85
```

**始终压全片**，不做"只压一小段"的预览（见 §6.6）。

**QC 是强制环节，且必须肉眼看图**：libass 找不到字体时**不会报错**，会静默回退导致中文变方块。只看 ffmpeg 返回码等于没验。脚本会在每行中点抽一帧。

---

## 4. 核心算法：跨语言音素 DTW

### 4.1 特征空间

把日文罗马音音节和中文拼音音节都拆成 `(声母/辅音, 韵腹/元音, 韵尾)` 三元组，每一维用发音特征向量表示：

| 维度 | 编码 |
|---|---|
| 辅音 · 发音部位 | 0 双唇 · .15 唇齿 · .3 齿龈 · .45 龈后/卷舌 · .6 龈腭 · .7 硬腭 · .85 软腭 · 1.0 声门 |
| 辅音 · 发音方法 | 0 塞音 · .25 塞擦 · .5 擦音 · .75 鼻音 · .9 边/闪 · 1.0 半元音 |
| 辅音 · 清浊 | 0 / 1 |
| 元音 · 高低 | 1 高 → 0 低 |
| 元音 · 前后 | 0 前 → 1 后 |
| 元音 · 圆唇 | 0 / 1 |

距离 = `0.40 × 辅音距 + 0.45 × 元音距 + 0.15 × 韵尾距`。无元音的音节（促音、拨音）只比辅音和韵尾。

几个需要单独处理的点：
- 日文的 `u` 实际是**非圆唇**后高元音，不能直接套拼音的 `u`
- 拼音的 `sh/ch/r` 是**卷舌**，与日文的 `sh/ch` 发音部位不同（龈腭），要分开编码
- 拼音的 `j` 是不送气**清**塞擦，不是浊音

**自检**（`python phonetics.py`）：真实空耳对应的距离应显著低于随机对照。

| 对 | 距离 |
|---|---|
| 真实对应（三例） | 0.000 |
| 随机对照（三例） | 0.41 – 0.68 |

### 4.2 子序列 DTW

```
D[0][j] = cost[0][j] + j × START_SKIP_PENALTY      ← 起点半自由
D[i][j] = cost[i][j] + min(
              D[i-1][j-1],                          ← 1 字 ↔ 1 音节
              D[i-1][j]   + STEP_PENALTY,           ← 多字共享一个音节
              D[i][j-1]   + STEP_PENALTY )          ← 一字吸收多个音节
终点     = argmin_j D[M-1][j]                       ← 终点自由
```

三个设计决策，每一个都对应一次实际失败：

**(a) 为什么是子序列而非整段 DTW**
整段 DTW 会强迫最后一个空耳字去匹配全曲最后一个音节。空耳通常只覆盖歌曲的一部分（本次 14 行覆盖 25.7–80.4s，约全曲三分之一），强行拉伸会把中间全部变形。

**(b) 为什么非对角步要有惩罚**（`STEP_PENALTY = 0.18`）
没有惩罚时 DTW 会退化 —— 让一个音节吸收掉十几个汉字，在代价矩阵上确实更省。惩罚逼它保持接近单调一一对应。

**(c) 为什么起点不能完全自由**（`START_SKIP_PENALTY = 0.15`）
这是本项目最有价值的一次调试。

初版用 `D[0][j] = cost[0][j]`（完全自由起点）。结果**整体晚了一整句**：第 1 行的首字对上了第一句的**最后一个音节**。

判断"是搜索问题还是打分问题"的方法：**固定成逐字一一对应，扫 40 个候选起点，各算平均代价**。

| 起点 | 平均代价 |
|---|---|
| **0（语义正确）** | **0.2108** ← 全场最低 |
| 7 | 0.2843 |
| 10（DTW 实际选的） | 0.2987 |
| 其余 37 个 | 0.30 – 0.47 |

结论明确：**打分函数是对的，是搜索飘了**。跳过 198 个音节和跳过 0 个一样便宜，而跨语言音素距离本身较宽松，一个错位起点只要总代价低一丁点就会赢。空耳是从歌曲开头写起的，"跳得越多越可疑"这个先验必须**编进代价**。

修复后第 1 行代价 0.250 → **0.211**。

### 4.3 幽灵音节门控

Whisper 在纯器乐段会产生**幻觉文本**。强制对齐只好把这些不存在的音节硬塞进静音区，产生过"14 个音节挤在 0.46 秒内"这种物理不可能的结果。

按**证据**剔除，不是硬编码偏移量：以 50 ms 为粒度算人声轨 RMS 包络，取峰值的 2% 为门限，凡是整个时段都在门限以下的音节全部丢弃。

本次实测：**745 个音节中剔除了 196 个**（26%），全部落在人声 RMS 恒为 0.00000 的区域。

### 4.4 验收标准

| 指标 | 阈值 | 本次结果 |
|---|---|---|
| DTW 平均每字代价 | < 0.35 | 0.236 |
| 代价 ≥ 0.35 的行数 | 0 | **0 / 14** |
| 语义抽查 | 音译对应正确 | 3 / 3 |

**语义抽查**是最强的验证：挑几个空耳中明显在模仿特定日文词的位置，检查它是否真的对上了那几个音节。本次三处全对。其中一处的画面内容恰好与该词的语义一致，属于额外的旁证。

`align_report.md` 会自动列出「对得最勉强的 15 个字」，供作者据此微调自己的用字。

---

## 5. 特效系统

`effects.py` 提供 **32 种**逐字特效，`render_effects.py` 为每种渲染一份样片。

**为什么需要 PyonFX**：单纯的 `\kf` 只能改颜色。要做"弹出/跳动/旋转/散开"，必须知道每个字在屏幕上的**精确坐标**才能给它单独发一条带 `\pos` 的 Dialogue。PyonFX 用系统字体度量算出每个音节的 `x/left/width`，实测在 Windows + 微软雅黑 + 中文下正常工作。

**三段式结构**：每个字拆成三条 Dialogue —— `pre`(未唱) / `hit`(正在唱) / `post`(已唱)，动画写在 `hit` 段的 `\t()` 里。这比在单行内塞内联 `\t` 可控得多。

统一用 `\an5`（中心对齐）+ 每个特效自己给的 `\pos` 或 `\move`，这样缩放/旋转都以字心为原点，不会飘。

分类：

| 类 | 数量 | 例 |
|---|---|---|
| 填充/基础 | 4 | 扫光、淡变色、半透明显现、描边闪 |
| 缩放 | 6 | 弹出、大弹出、越唱越大、砸落、Q弹、心跳 |
| 位移 | 6 | 上跳、天降、升起、左滑入、波浪、抖动 |
| 旋转 | 5 | 转圈、歪头、3D 上下翻、3D 左右翻、旋转弹出 |
| 模糊/发光 | 4 | 发光、虚→实、霓虹、闪白 |
| 颜色 | 4 | 彩虹、火焰、寒冰、鎏金 |
| 组合/主题 | 3 | 弹出+发光、打字机、烟花炸开 |

---

## 6. 故障模式手册

每一条都是本项目**实际踩过**的，附现象、根因、修法。

### 6.1 torch 被静默降级成 CPU 版
- **现象**：`torch.cuda.is_available() == False`；`libtorchaudio.pyd` 加载失败
- **根因**：先装 CUDA torch、后装 `audio-separator[gpu]`，pip 从默认 PyPI 把 torch 覆盖成 CPU 版，而 torchaudio 还是 CUDA 版 → ABI 不匹配
- **修法**：见 §2.3。**装完必须验 `torch.cuda.is_available()` 并跑一次真实矩阵乘**

### 6.2 torchcodec 加载失败
- **现象**：`Could not load libtorchcodec ... We support versions 4, 5, 6, 7, and 8`
- **根因**：BtbN 的 `master` 构建是 `avcodec-63`（FFmpeg 9 开发版），超出支持范围；且静态 `ffmpeg.exe` 根本不提供动态库
- **修法**：另下 `ffmpeg-n7.1-latest-win64-gpl-shared`，用 `os.add_dll_directory()` 指过去

### 6.3 强制对齐产出空结果且不报错
- **现象**：喂日文歌词进去，转录为空，无任何错误
- **根因**：`yohane.lyrics.normalize_uroman()` 执行 `re.sub("([^a-z'\n ])", " ", text)`，汉字假名被整个删掉。其对齐模型 MMS_FA 的词表只有 **27 个拉丁字母 + blank**，从构造上就只接受罗马音
- **本地复现**：`normalize_uroman('打上花火')` → `''`，`normalize_uroman('うちあげはなび')` → `''`
- **修法**：先用 cutlet/pykakasi 转罗马音

### 6.4 供应链陷阱
- **现象**：`pip install "yohane[cli]"` 装完看着正常，运行时 `ImportError`
- **根因**：`pyproject` 用 `[tool.uv.sources]` 把 `vocal-remover` 指向 Japan7 的 fork，而 **pip 不认这个字段**，会从 PyPI 装到一个同名的无关 stub（v0.0.6，homepage 指向 `pypa/sampleproject`）
- **修法**：不带任何 extra 安装，或改用 uv

### 6.5 yohane 的输出路径陷阱
- 没有 `--output` 参数，输出路径由**输入音频文件名**推导
- `with_suffix()` 只剥最后一段 → `song.vocals.wav` 会被当成 stem `song`，静默覆盖别的结果
- **修法**：喂进去的文件名不能带多余的点
- 另：路径不存在时它**不报错**，而是把字符串丢给 yt-dlp 去下载

### 6.6 裁剪导致字幕整体偏移
- **现象**：预览片里字幕比画面晚了一大截
- **根因**：`-ss` 放在 `-i` **之前**做输入定位时，ffmpeg 会把输出时间戳归零，而 `.ass` 里是**绝对时间**
- **实测**：第 1 行应在 25.72s，却出现在预览的 26.69s（= 绝对 50.4s），差值**正好等于 23.7s 的裁剪起点**
- **修法**：始终压全片（全片编码在 12x 实时下只要约 25 秒），要短片段就从成品**流拷贝**。若确实要渲染片段，则生成样片专用 `.ass` 时把所有时间**整体减掉窗口起点**，两边都从 0 开始

### 6.7 NVENC 打不开
- **现象**：`Driver does not support the required nvenc API version. Required: 13.1 Found: 13.0`
- **根因**：ffmpeg master 构建要求驱动 ≥ 610，实际驱动 591.86
- **修法**：改用 FFmpeg **7.1** 的 `ffmpeg.exe` 做编码（实测可用）

### 6.8 ffmpeg filtergraph 的 Windows 路径
- **现象**：`No option name near '/Windows/Fonts'`
- **根因**：filtergraph 里盘符的冒号需要多层转义
- **修法**：把 cwd 切到 `.ass` 所在目录，filter 里只写文件名；`fontsdir` 干脆不传，libass 在 Windows 上本来就走系统字体查找

### 6.9 transformers ASR 要求裸 ffmpeg
- **现象**：`ffmpeg was not found but is required to load audio files from filename`
- **根因**：传文件路径时内部走 `audio_utils.ffmpeg_read()`，调的是裸 `ffmpeg` 命令
- **修法**：自己用 soundfile 读成 numpy 数组，传 `{"raw": ndarray, "sampling_rate": 16000}`，整条依赖直接消掉

### 6.10 库模块污染 stdout
- **现象**：`ValueError: I/O operation on closed file`
- **根因**：库和调用方各自包了一层 `io.TextIOWrapper(sys.stdout.buffer)`，两层套同一个 buffer，先被 GC 的那层把 buffer 关掉
- **修法**：库模块**绝不碰 `sys.stdout`**，中文输出统一靠 `PYTHONIOENCODING=utf-8`

### 6.11 SSH 速率限制
- **现象**：`kex_exchange_identification: read: Software caused connection abort`，全部文件同步失败
- **根因**：同步脚本对每个文件开 2 条 ssh（推送 + 校验），三十几个文件 = 六十多条连接
- **修法**：`tar czf - <files> | ssh host "tar xzf - -C <dest>"`，**单条连接**。触发限流后需要冷却，不要快速重试

### 6.13 相邻行字幕重叠
- **现象**：相邻两句的字幕偶尔同时出现在屏幕上，叠在一起
- **根因**：整行的提前出现量 / 延后消失量用了**固定值**（各 350–400 ms）。而对齐结果里相邻行的实际间隔差异极大
- **实测证据**：13 对相邻行里 **8 对间隔 < 0.8 s**，其中 1 对间隔**正好为 0**

  | 间隔 | 对数 |
  |---|---|
  | 0.00 s | 1 |
  | 0.22 – 0.46 s | 7 |
  | > 0.9 s | 5 |

- **修法**：按邻行的**实际间隔**逐行收缩，不能用固定值

  ```python
  lead_in  = min(LEAD_IN,  gap_prev * GAP_SHARE)
  lead_out = min(LEAD_OUT, gap_next * GAP_SHARE)
  # GAP_SHARE = 0.40 —— 两行各让 40%, 合计 80%, 仍留 20% 空隙
  ```

- **验收**：`make_ass.py` 会自己扫一遍显示区间并打印「重叠行对」，必须为 0；再抽间隔最小那一对的前后两帧肉眼确认

### 6.14 stdout wrapper 相撞（比 §6.10 更广的版本）
- §6.10 的结论原本写作"**库**模块不得碰 `sys.stdout`"，范围定窄了
- 后来 `render_effects.py`（一个**脚本**）被 `make_mixed.py` 当模块 import，同样的错误又犯了一次
- **修正后的规则**：**任何可能被 import 的文件**都不得触碰 `sys.stdout`，不区分"库"还是"脚本"

### 6.15 砍 UP 主自制片头
B 站转载常带一段 UP 主自己做的片头（本片是 2.85 秒的 `DRV HD REMASTER TEAM PRESENTS`）。

**定位切点**：用 `fps=N,tile=RxC` 拼一张缩略图矩阵，一次看完一整段。先 1 秒粒度扫全局，再 0.04 秒（逐帧）粒度定边界。本片实测：2.78 s 仍有文字残影，2.82 s 已完全干净，切点取 **2.85 s**。

**不能用流拷贝**：本片关键帧只在 **0 / 5.005 / 10.010 秒**（GOP = 5 s），2.85 s 处没有关键帧。`-ss 2.85 -c copy` 会吸到 0 s（带上整个片头）或 5.005 s（切掉原 MV 的官方标题卡）。

**也不该去砍已压好的成片** —— 那是二次编码。正确做法是**从原始 MV 重压一次**，同时把 `.ass` 前移相同量：

```python
burn(mv, shift_ass(ass, tmp, 2.85), out, trim=2.85)
#     -ss 2.85 让输出时间戳归零, 字幕也从 0 起算 -> 对齐
```

只裁视频不移字幕，就会重演 §6.6 那个偏移。

**验收**：① 时长差应等于裁剪量（实测 298.30 → 295.46，差 2.84 s，符合帧量化）；② 抽新片头前 3 秒确认无残留；③ 抽任意一行确认字幕仍与画面对得上。

### 6.12 audio-separator 的静默 OOM
- **现象**：只报 `Separation produced no output files`
- **根因**：`separate()` 把每个文件包在 `try/except` 里，真正的 `torch.OutOfMemoryError` 只出现在 traceback
- **修法**：诊断时加 `--log_level debug`
- 另外两个坑：`--mdxc_segment_size` 单独给**无效**，必须同时给 `--mdxc_override_model_segment_size`；`--model_file_dir` 默认是 POSIX 的 `/tmp/...`，Windows 上会落到当前盘根目录

### 6.16 别自己算字幕坐标 —— 用渲染器的内建能力
做「重点词下划线」时，为了知道每个字的 x 坐标，先后试了两条路，**都失败**：

| 办法 | 结果 |
|---|---|
| 用 PyonFX 量字体度量 | 它**忽略内联 `\fscx`**，把留白按 `\h` 原生 68.6 px 计，而 libass 实际渲染 30 px → 线整体左移、且逐组右漂（原生抽帧实测：第 1 组左偏 ~150 px，第 5 组右偏 ~90 px） |
| 自己按「字宽 61.1 + Spacing 2」解析建模 | 逐字画彩色标定框后发现模型整行比实际宽约 80 px，仍对不齐 |

**共同病根：两条路都在猜渲染器会怎么排版。**

**修法**：用 ASS 内建的 `\u1` / `\u0`。渲染器自己排版、自己画线，位置不可能错，一行代码，零坐标。

**可迁移的教训**：当一个效果需要「知道渲染结果的几何」，先查格式有没有内建标签。自己复算排版等于把渲染器的活重做一遍，而你永远拿不到它的全部规则（字距、标点宽度、内联缩放对 advance 的影响……）。**同理，`\1c` / `\2c` 是逐音节生效的，所以「按词着色」根本不需要坐标 —— 优先选不依赖几何的方案。**

**配套的核对方法**（这次是被自己的 QC 图骗了）：位置相关的东西抽帧一律 `crop=1920:170:0:880` **原生分辨率裁字幕带，绝不缩放**。缩到 2/3 的对比图看不出几十 px 的偏移。判不准就画标定框（每个字一个色块，红绿交替），对不齐一眼看穿。

### 6.22 产物路径必须唯一 —— 这一轮最贵的教训
- **现象**：作者反馈的 6 个问题里有 5 个「已经修好了」，全部白跑一轮
- **根因**：压制输出写 `dashanghuahuo_soramimi_<配色>.mp4`，而交付件叫 `FINAL_*`，**两个名字**。我让作者看 `FINAL_*`，那是上一版
- **修法**：`burn.py` 只写 `FINAL_dashanghuahuo_full.mp4`，并顺手切好 90s 版
- **教训**：**反馈回路指向错误的对象，比任何算法 bug 都更浪费时间**。凡是要拿给人看的产物，路径必须唯一且固定；「最新的那个」不能靠人记名字

### 6.23 位置偏了，先数自己发了多少字符
「重点词加下划线」这一个需求，前后返工 **5 次**：

| 尝试 | 结果 |
|---|---|
| `\u1` + 原字 | 位置对，但贴字 + 逐字断开 |
| 全角下划线字符 U+FF3F | 高度可控，仍断开（该字形两端留白，墨迹只占字宽约六成） |
| `\p1` 内联绘图**顶替**那几个字 | 连续了，**左偏 30px** |
| 绘图 + 负 `\fsp` 抵消前进宽 | **左偏更多** |
| `\u1` + 全角空格 U+3000 + `MarginV` 下移 | 位置对、连续、无残影 ✅ |

**前四次都在猜渲染器的排版行为**（正是 §6.16 记过的坑，又踩了三遍）。而真正的 bug 在自己的代码里：下划线层的循环用「标志位 + `continue`」写，**词尾标点被输出两次**（多一个全角字宽 → 整层左移半个字），**非下划线字的留白从未输出**（有断点的行会错位）。

- **不变量**：叠加层与正文层**每一格恰好输出一次**，总宽严格相等 → 各自居中 → 必然对齐
- **教训**：位置偏了，**先怀疑自己生成的内容，再怀疑渲染器**。数一遍输出的元素个数，比换三种画法快得多
- 附带两条：`\u1` 的线与字身共用颜色/透明度，没法只藏字不藏线 → 用 U+3000 占位；垂直位置用 Dialogue 自己的 `MarginV` 调，别用 `\pos`（`\pos` 还要给 x，又回到算坐标的死路）

### 6.24 装饰标签作用于整段 → 标点必须单独成段
- **现象**：作者反映「逗号也被划了线」
- **根因**：`\u1` / `\1c` 作用于**整个文本段**，而标点跟在字后面放进了同一段
- **修法**：标点永远单独成段，装饰只落在字上，标点退回本行基础配色
- 同一缺陷在着色路径上也存在（着色词若带标点会一起被染色），只是当时没有带标点的着色词，属于**潜伏**。一并修掉
- **教训**：一个「作用于整段」的属性，就要求段的切分与语义单元一致。发现一处，要去查所有用同类属性的地方

### 6.25 QC 截图本身也会骗人
连续三次把「位置对」判成「位置错」或反之，全部是截图方式的问题：

- 裁剪带横向偏移（`crop=...:0:600:...`）却按图内坐标对照 → 偏移量被算进去了
- 裁得太窄，没有足够的参照物
- 缩放过的对比图（1920→1280）看不出几十 px 的偏移

**规程**：位置相关的核对一律 `crop=1920:H:0:Y`，**全宽、零横向偏移、不缩放**。判不准就构造「错了就藏不住」的诊断——例如临时把整行都加上装饰，看它是否严丝合缝覆盖整行。

### 6.19 1:1 继承音节时长 → 有的字「瞬间弹出」
- **现象**：作者反馈某些字「过得极其快」，不像在刷
- **实测**：单字扫光时长从 **0.000 s** 到 1.16 s，中位 0.32 s。`\kf0` 根本不是刷，是跳。全曲 10 个字低于 0.14 s
- **误诊排查**：作者猜是「两个字共用一个音、被强行断音」。实测**只有 4 个字**落在共用簇里（两处叠字），不是主因。真正的驱动是**日文音节本身时长极不均**，1:1 映射原样继承了
- **修法**：给每字扫光时长设**下限**，缺的从「超出下限的部分」按比例借，**整行总长不变**（逐行断言）。迭代 6 轮，因为捐助者自己可能掉到下限以下
- **关键取舍**：**只抬下限，不压上限**。长音的「拖住」是真实的节奏信号，压掉就把节奏抹平了
- **加重因素（自找的）**：留白按宽度比例分走扫光时间，带留白的字只剩 ~70% 时间在自己身上。光走在空白里是看不见的 → 给留白的时间**封顶** `GAP_MAX_SEC`

### 6.26 扫光速度的突变 —— 限幅胜过全局匀速
- **现象**：作者反映「同一句里不同节奏的字之间，刷的 transition 很唐突」
- **量化**：每个字宽度一样但时长不同 ⇒ **速度**不同。实测**相邻字速度最大跳变 4.1 倍**
- **两条路的实测代价**（同样把跳变压到 1.5x 时，「字被点亮 vs 该被唱到」的最大偏移）：

  | 方案 | 偏移 |
  |---|---|
  | 全局混合（每个字都往「按宽度匀速」拽） | **0.62 s** |
  | **速度限幅**（只动跳变过大的相邻对） | **0.20 s** |

- **排除的直觉答案**：完全匀速跳变降到 1.0x，但偏移达 **0.88 s** —— 光和人声脱节近一秒，跟唱会错乱
- **选定**：限幅 1.7x（跳变 4.1→1.7，偏移仅 0.17 s）
- **教训**：这是控制系统里的 **slew rate limiting**——信号整体形状是对的，只是变化率不可实现时，**削变化率而不是重塑信号**。全局混合动了所有样本，限幅只动违规的那几对，代价相差 3 倍
- **坑**：限幅是**成对**调整的，会把某个字压回下限以下（实测 0.20 s < 下限 0.22 s）。必须与下限**交替迭代**到两个约束同时成立

### 6.27 作者写在歌词里的空格不该变成屏幕上的间隔
- **现象**：某行多出一个没人要的间隔
- **根因**：作者在词句之间写的空格是给自己断句看的，被原样渲染成了一整个字宽的留白
- **修法**：渲染与标注定位时都 `strip()` 掉字面空格。留白只来自作者显式的断点标注
- **教训**：输入里的**排版性字符**（空格、制表符）承载的是作者的编辑意图，不是内容。要么显式解释，要么显式丢弃，不能默认透传

### 6.20 自动规则会劈开自然词
- **现象**：作者发现「阁」「塔」被留白分开了，而他要的是「疙瘩」整体与后文分开
- **根因**：「被拖长的字后面自动留白」这条规则**不认识词**。全曲 18 处触发，其中 4 处劈开了真正的中文词
- **修法**：① 已标注为词的内部一律不插留白；② 把被误伤的词补进词表（只留白、不给颜色）
- **教训**：任何「按信号自动插入视觉分隔」的规则，都必须有一层**语义否决**。信号（时长）和语义（词边界）是两个正交的维度，前者不能单独决定后者

### 6.21 同一行的多条标注只取了第一条
- **现象**：词表里 L09 写了两行（一条审美标注、一条防误伤补丁），结果第一条被静默丢弃
- **根因**：解析时 `break` 在首次匹配
- **修法**：**累加所有匹配行**，不 break。分散书写是正常需求（不同来源的标注各写各的）
- **教训**：配置解析遇到重复键，默认应当是**合并**而非「先到先得」，尤其当两条来自不同意图时——静默丢弃不会报错

### 6.18 `\1c` 只改颜色、不改透明度 —— 且「样片对、成片错」
- **现象**：作者反馈「整体字幕比以前暗淡很多」，而实验样片看着是好的
- **根因**：做「未唱压暗」时把 `C_UNSUNG` 改成半透明（`&H96FFFFFF&`），但 `make_ass.py` 的 Style 行里 **PrimaryColour 和 SecondaryColour 用的是同一个常量**：

  ```
  Style: Sora,{FONT},{FONT_SIZE},{C_UNSUNG},{C_UNSUNG},...
                                 ↑Primary  ↑Secondary
  ```

  每个字的 `\1c` **只覆盖颜色，不覆盖透明度**，透明度来自 Style 的 PrimaryColour。于是**唱过的部分也变成 59% 透明**，整条字幕发灰。
- **为什么没在 QC 里发现**：实验脚本 `rhythm_variants.py` 的 header 把 Primary 硬写成不透明色，只有 Secondary 用了变量。所以**样片正常、成片异常**，而我一路抽帧核对的都是样片。
- **修法**：`C_SUNG`（不透明）与 `C_UNSUNG`（半透明）分成两个常量，Style 里各归各位。
- **可迁移的教训**：**别用同一个常量填语义不同的两个字段**——一旦其中一个的含义变了（这里是加了 alpha），另一个会被静默连坐。还有：**实验脚本和生产脚本各有一份 header 时，QC 必须落在生产产物上**，不然验的是另一个东西。

### 6.17 ASS 的行内标签是「持续生效」的
`\fscx` / `\2c` / `\u` 一旦设置就一直作用到行尾，不会在下一段自动复位。

- **踩到的**：给留白用的 `\h` 套了 `\fscx44` 压窄，结果**第一个留白之后的所有字都被横向压扁**。
- **返回码是 0**，只有抽帧才看得见（对比图上「怀念/记载/吾待」明显比「似那么」小一圈）。
- **修法**：每个字的标签块里显式写全 `\fscx100`、`\2c...`、`\u0`，不依赖「上一个没设过」。

### 6.28 两个「实测」常量名不副实 —— 一个差 4.4 倍，一直没被发现
做对照版式要跨图层按格对齐，第一次需要**绝对**宽度，才逼出了这个问题。

`make_ass.py` 里两个注释写着「实测」的常量：

| 常量 | 代码里写的 | `measure_metrics.py` 渲染实测 | 结论 |
|---|---|---|---|
| `GLYPH_W` | 61.1 | 字形 **59.1**；字距（字形 + Spacing 2）**61.1** | 名字错了，值其实是**字距** |
| `SPACE_ADV` | 68.6 | `\h` = **15.6** | **差 4.4 倍**，来源不明 |

为什么一直没暴露：这两个值在 `make_ass` 内部**只参与比值**（速度限幅的相对权重、
时长按宽度分配），整体缩放一个常数不改变任何比值；而留白 `\h` 的 `\fscx` 在主行和
下划线层用**同一个公式**算，两层照样对齐。所以画面一直是对的。

唯一被咬到的是 `GAP_BREAK = 34`：`\fscx` 取整后 libass 实际画出
`15.6 × 50% + 2 ≈ 9.8px`，**只有标称值的 29%**。作者对当前观感是满意的，所以本轮
**没有改动 make_ass**（改了会让所有断点突然变宽 3 倍）。但要调断点宽度时必须知道：
**这个旋钮的刻度是错的。**

**可迁移的教训**：注释写「实测」不等于可信 —— 它没留测法，就无法复核。度量类常量应当
由一个**可重跑的脚本**产出（现在是 `measure_metrics.py`：渲染 → 数像素 → 两串相减
消掉描边外扩），而不是手抄进源码。更普遍的一条：**自洽的错误最难发现** —— 全链路用同一个
错值时结果完全正确，只有当第三方（这里是注音层）要跟它对齐时才会现形。

### 6.29 注音层：仍然不算坐标，但要按「格」而不是按「字」
注音层沿用下划线层的原理（§6.16）：**逐格前进宽度与主行相等 → 两层各自居中 → 必然对齐**，
全程零绝对坐标。三个新踩的点：

1. **短音不能拉伸填格。** 下划线可以按格宽拉伸，注音不行 —— 单音节 `a` 拉到整格宽会变成
   一个畸形的宽字母。所以每格是 `[左垫片][原尺寸罗马音][右垫片]`；垫片用 **U+3000**
   （恰好 1 em，是这套字体里唯一不必实测就敢信的宽度），不用 `\h`（宽度得靠实测）。
2. **标点必须自己占一格。** 第一版把词尾标点并进了字格（`target = PITCH×(1+标点数)`），
   注音被推到字和标点的中间；更糟的是**逗号之后每一格都整体错开一整格**。标点不发音，
   得给它一格空的。这是 §6.24 的同一类错误换了个地方复发。
3. **诊断帧要铺纯黑。** 位置靠数像素判，而 L15 那帧背景是一盏亮灯，阈值分割会把整片连成
   一块、竖线根数直接数错。用 `drawbox=...:t=fill` 刷黑；**不要**换成 lavfi 纯色源 ——
   纯色源没有真实时间戳，`.ass` 的绝对时间对不上，出来是一张全黑的空帧。

**核对方式**：`check_ruby_align.py` 从诊断帧求每根竖线的墨迹质心，与排版公式算出的格心
逐格相减。L15 + L03 共 25 格，**最大偏差 0.87px**。这是数值核对，不是看图 —— 肉眼觉得
竖线「偏窄」其实是错觉：线落在格**中心**，而字的墨迹几乎铺满整格还带 4px 描边，所以竖线
跨度天然比文字跨度窄一个字宽（§6.25 的老毛病，这次靠数字避开了）。

### 6.30 对照版式：不要重画主行
做 23 个版式候选时，最容易的做法是「照着 make_ass 再写一个简化版」。**不要。**
`layout_variants.py` 直接调 `make_ass.build()` 拿到成品同款的 Dialogue 行，
只用一个 `retime()` 改 Layer / MarginL / MarginR / MarginV **四个字段**，文本一字不动。

这样保证了：候选图里的扫光、断点、着色、下划线和成片**逐帧一致**；
以及 —— 更重要的 —— 不会出现第二份排版实现，日后一改就分叉（§6.18 已经吃过一次
「实验脚本和生产脚本各有一份 header」的亏）。

---

## 7. 复现步骤

```bash
export PYTHONIOENCODING=utf-8
PY=C:/Users/59827/karaoke/venv/Scripts/python.exe
cd code

$PY bili_search.py "<歌名>" 2      # 1. 检索
$PY probe_candidates.py             # 2. 候选元数据
$PY frame_grab.py                   #    抽帧实测硬字幕  ← 人工看图选片
                                    # 3. 下载（见 §阶段3）
$PY separate_vocals.py              # 4. 抽音轨 + 人声分离
$PY transcribe_vocals.py            # 5. ASR + 罗马音
$PY force_align.py                  # 6. 音节级强制对齐
#    把空耳歌词放进 02_lyrics/soramimi_user.txt
$PY soramimi_align.py               # 7. 子序列 DTW           ← 看 align_report.md
$PY make_ass.py                     # 8. 字幕: 滚动扫光 + 逐字配色（默认路径）
$PY render_effects.py               #    可选支路: 32 种逐字动画样片 ← 人工挑
$PY burn.py ice_rainbow --trim=2.85 # 9. 压制 + QC 抽帧       ← 人工看图
bash sync_to_server.sh              # 10. 交付
```

**人工只出现在 4 个位置**：选 MV、提供空耳、挑特效、看成片。其余全自动。

换歌时需要改的只有：检索关键词、空耳文本。其余脚本不动。

---

## 8. 验证清单

上线前逐条过：

- [ ] `torch.cuda.is_available()` 为 True，且真实跑通一次矩阵乘
- [ ] 人声分离后，**纯器乐段人声轨 RMS 归零**
- [ ] 罗马音双路交叉验证（**忽略空格**比较字母序列）
- [ ] 强制对齐峰值显存**实测**打印，未 OOM
- [ ] 幽灵音节剔除数量合理（本次 196/745）
- [ ] DTW 平均代价 < 0.35，且**逐行**无超阈值
- [ ] 语义抽查：几处明显的音译对应确实对上
- [ ] 抽帧**肉眼确认**中文无方块、无超框、无遮挡关键画面
- [ ] 抽帧时间与字幕内容对得上（验证无 §6.6 的偏移）

---

## 9. 已知局限

1. **对齐器不分块**，VRAM 随音频时长线性增长。5 分钟曲子实测 7.17 GB，8 GB 卡已接近上限，更长的曲子需要分段处理。
2. **`\kf` 的最后一个音节系统性偏短**（yohane 自身的已知问题，回退用自己的 `end_s` 而非 snap 到下一个起点）。已选用作者推荐的微调模型缓解。
3. **和声/重叠人声段**强制对齐会退化，这是 CTC 强制对齐的固有限制。
4. **音素特征表是手工调的**，目标是"跨语言粗粒度相似"，不追求语音学精确。若换语言对（如英日空耳）需要重调。
5. 幽灵音节门控**假设幻觉都落在静音区**。若 ASR 在有人声处产生幻觉，门控无能为力，只能靠 DTW 代价异常暴露。
