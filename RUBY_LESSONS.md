# 逐字注音（ruby）— 踩过的坑、怎么改、以后怎么防

2026-08-06 一轮密集返工的完整记录。按「具体例子 → 当时怎么改 → 高一层的问题是什么 → 以后具体注意什么」组织。

---

## 0. 一句话总纲

> **注音的切分服从空耳，不服从日文词界。**

空耳的全部依据是**汉字自己的读音**像那个日文音。所以判断一处注音对不对，
不需要懂日文——把汉字的拼音念出来，对不上就是错的。这条是作者给的，
也是下面绝大多数错误的统一判据。

---

## 1. 源头错了，下游全错

| | |
|---|---|
| **例子** | 「启明」标成 `kun`，应为 `kimi` |
| **当时怎么改** | 不是改显示层。真歌词到位后，把它送回流水线源头重跑：<br>`romaji_from_ref.py → force_align.py → soramimi_align.py` |
| **高一层** | 注音是**派生物**。真数据到位时，要问的不是「显示对不对」，而是**「它是从哪儿派生的，那个源头更新了没有」**。 |
| **以后注意** | 任何「作者补充了权威数据」的时刻，先画一遍数据流：新数据进入的是哪一环？它下游的所有产物都要重跑，不能只补最末端。 |

同一个坑的第二次：`ref_ja.txt` 填了真歌词，但注音仍来自 Whisper 听写——
**参考行和注音是两条独立的路径**，我只更新了看得见的那条。

---

## 2. MeCab 的读音是猜的，而这个猜测喂给了对齐器

| | |
|---|---|
| **例子** | 「君」单独转是 `kimi`，放进整行被当后缀读成 `kun`；`を` 转成 `wo`，唱的是 `o` |
| **当时怎么改** | `02_lyrics/reading_overrides.txt`，每行 `汉字=かな`。在转罗马音**之前**把汉字换成假名——假名无歧义，MeCab 没有猜错的余地。<br>`君=きみ`（命中 4 处）、`を=お`（命中 14 处） |
| **高一层** | 一个**看似只影响显示的中间量**，实际是更上游环节的输入。错读音不只让屏幕上的字不对，**那一处的时间轴也是按错音对的**。 |
| **以后注意** | 遇到 NLP 组件的「自动判断」（分词、读音、词性），先问：它的输出还喂给了谁？只要喂给了别人，就要给作者一个覆盖入口，而不是等发现了再逐个打补丁。 |

---

## 3. 我的度量尺在「完全正确」时也读不到 100%

| | |
|---|---|
| **例子** | 报了「整体只有 78% 的注音音节是对的」。重跑源头后只动到 80%——因为它测的根本不是内容正确性 |
| **当时怎么改** | 换成**能判定的**测试：逐字注音是否为「真歌词强制对齐流」的子序列 → `True`。内容自此由构造保证 |
| **高一层** | 那个 78% 拿**对齐器的音节切分**去比**我自己正则切的 cutlet 输出**，两套口径不同；逐行比对还叠加了行界差异。**尺子本身有系统误差时，读数的变化量毫无意义。** |
| **以后注意** | 报任何百分比之前先问一句：**「这把尺子在『完全正确』的情况下读数是多少？」** 不是 100% 就先修尺子，别拿它下结论。 |

---

## 4. 测试夹具和真实内容不同形 ＝ 只证明了它自己没错

| | |
|---|---|
| **例子** | 给宽格子加「向邻居借宽度」，真实注音整行偏掉，而 `check_ruby_align.py` 仍报 **0.87px 通过** |
| **原因** | ZZ 诊断帧每格只发一根等宽的 `|`。全是窄 token，**「多拍格子太宽」这条代码路径根本不会被触发** |
| **当时怎么改** | 1. 回退借宽度（对齐是硬要求，压扁只是不好看，不能拿对齐换）<br>2. 加**解析式不变量检查**：每个注音组的中心必须等于它那个字的格心。与内容宽窄无关，那次回归会被一眼抓住 |
| **高一层** | 通过的测试给的是**虚假安全感**，比没有测试更危险——它让人停止怀疑。 |
| **以后注意** | 写测试时问：**这个夹具会走到我要测的那条分支吗？** 尤其是「只在极端输入下才触发」的逻辑（太宽、太长、空、有标点）。宁可加一条不依赖渲染的解析式不变量。 |

---

## 5. 同一类错误会在不同地方复发

| | |
|---|---|
| **例子** | 「标点必须自己占一格」这条，在三个地方各犯了一次：<br>① 下划线层把词尾标点发了两遍 → 整层左移半个字<br>② 注音层把标点并进字格 → 逗号之后每格错开一整格<br>③ 改「借宽度」时把格宽写成 `PITCH*(1+标点数)`，而标点另外还发了占位 → **宽度算两遍**，带标点的字之后整行右移 31.55px |
| **当时怎么改** | 每格只算**字**那一格宽；标点在别处单独发等宽占位 |
| **高一层** | 一个模型上的误解（「标点是字的一部分」）会在每个重写排版的地方重新长出来。**修好一处不等于修好这个概念。** |
| **以后注意** | 每次修完一个坑，去 grep 同一个概念在别处的写法。ENGINEERING.md 里给它一个编号，复发时引用编号，能看出它是第几次。 |

---

## 6. 「改完还要再判一次」的循环会自反馈

| | |
|---|---|
| **例子** | 促音合并后的 `tto` 是双辅音开头，不匹配我的单辅音正则 → 被当成裸辅音**又并一次** → 级联出 `ttohikattesaita` |
| **当时怎么改** | 改成单趟处理，合并结果**不回测**；正则同时放宽到 `^[a-z]*[aiueo]+$` |
| **高一层** | 「判定 → 修改 → 再判定」的循环里，如果修改后的形态不落在「已修好」的判定域内，就会无限自我触发。 |
| **以后注意** | 写这类清洗循环时，显式维护「已处理」状态，不要靠重新判定内容来决定是否已处理。 |

---

## 7. 字数和音节数不等时，DTW 会自作主张

| | |
|---|---|
| **例子** | L11 对齐流只有 7 个音节、8 个字 → **重复**用了一个 `te`；L15 末尾 `ta` 被**丢掉**，促音只好往回并成 `kat`，那个字变空 |
| **当时怎么改** | 一律交给 `R` 标注由作者定死。另外修掉「合并后留空字」——空格子在屏幕上是个洞，也违背「每个空耳字都该有它接的那个音」 |
| **高一层** | 算法在**信息不足**时不会报错，它会编一个。补的在哪、丢的在哪，只有作者知道。 |
| **以后注意** | 凡是「N 个输入映射到 M 个输出且 N≠M」的地方，都要有作者覆盖入口，并主动把「补/丢发生在哪里」列出来给人看，而不是静默处理。 |

---

## 8. 判据本身可以做成工具

作者一条条报「这里不对」效率很低。把判据（第 0 条）做成诊断即可批量找：

```bash
python make_ass.py --ruby-lines    # 逐行铺开，扫读音
python make_ass.py --ruby-report   # 按对齐代价排序，找配错字
python check_ruby_align.py         # 解析式不变量 + 渲染式核对
```

**注意两者分工不同**：`--ruby-report` 按对齐代价排序，找的是「配错字」；
而**读音错不错和代价无关**——错读音也可能配得很像，只能整行铺开对着听。

比对「汉字自己的读音 vs 分配到的音节」（复用 `phonetics.py` 的音素距离）能一次
抓出一批，例如「啊」被分到 `wo`——啊是 a，不可能是 wo，由此反查出 `を` 的读音问题。

---

## 9. 现有的作者覆盖入口

| 文件 | 前缀 | 管什么 |
|---|---|---|
| `soramimi_groups.txt` | `B` / `C` / `U` | 断点 / 着色 / 下划线（作者原创，随仓库分发） |
| `soramimi_ruby.txt` | `R` | 注音改派（值是原词罗马音，**不入公开仓库**） |
| `reading_overrides.txt` | `汉字=かな` | 读音覆盖（影响罗马音**和**对齐，改完要重跑对齐） |

`R` 与 `B/C/U` 分文件不是洁癖：混在一份文件里，一次 commit 就把原词读音带进公开仓库了。

---

## 10. 改完要重跑什么

```bash
# 只改 R（注音改派）—— 不必重新对齐
python make_ass.py --layout=E3

# 改了 reading_overrides.txt —— 必须从源头重跑
python romaji_from_ref.py && python force_align.py && python soramimi_align.py
python make_ass.py --layout=E3

# 都要以这三条收尾
python check_ruby_align.py          # 不变量 + 渲染核对
python make_ass.py --selftest       # 无版式路径未漂移
python burn.py ice_rainbow --layout=E3 --trim=2.85 --preview=90
```
